{
  "name": "ds-coding-agent",
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "start": "bun run index.ts",
    "test": "bun test",
    "typecheck": "tsc --noEmit",
    "lint": "oxlint",
    "build:bin": "bun build --compile --outfile dist/ds-coding-agent index.ts"
  },
  "dependencies": {
    "@ai-sdk/deepseek": "^2.0.35",
    "ai": "^6.0.184",
    "boxen": "^8.0.1",
    "picocolors": "^1.1.1",
    "zod": "^4.4.3"
  },
  "devDependencies": {
    "@types/bun": "^1.3.14",
    "oxlint": "^0.16.0",
    "typescript": "^5.7.0"
  }
}
